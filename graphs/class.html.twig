{% extends 'layout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link href="//api.joomla.org/css/jquery.iviewer.css" rel="stylesheet" media="all" />
{% endblock %}

{% block content %}
    <div class="row-fluid">
        <div class="span12">
            <div class="well wrapper">
                <div id="viewer" class="viewer"></div>
            </div>
        </div>
    </div>

    <script src="//api.joomla.org/js/jquery.iviewer.min.js" type="text/javascript"></script>
    <script type="text/javascript">
        $(window).resize(function(){
            $("#viewer").height($(window).height() - 260);
        });

        $(document).ready(function() {
            $("#viewer").iviewer({src: "{{ path('classes.svg') }}", zoom_animation: false});
            $('#viewer img').bind('dragstart', function(event){
                event.preventDefault();
            });
        });
    </script>
{% endblock %}
